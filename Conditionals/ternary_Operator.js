
// --------------- Ternary Operator (? :)--------------
// Syntax:
// condition ? expression_If_True : expression_If_False;

const userAge = 20;
const message = userAge >= 18 ? "Can vote" : "Cannot vote";
console.log(message); 