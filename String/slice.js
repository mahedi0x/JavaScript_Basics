const topic = "javascript";
const sliceTopic = topic.slice(4, 10);  // =========== .slice(startIndex, endIndex): Extracts a section of a string and returns it as a new string. =============
console.log(sliceTopic); 
// Extracts from index 4 up to (but not including) 10






